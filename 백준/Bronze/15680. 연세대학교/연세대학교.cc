#include <iostream>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int n;
    cin >> n;
    if (n) {
        cout << "Leading the Way to the Future";
    }
    else {
        cout << "YONSEI";
    }

    return 0;
}